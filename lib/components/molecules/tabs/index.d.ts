import { FC, ReactNode } from 'react';
interface Props {
    tabs: Array<{
        label: string;
        component: ReactNode;
    }>;
    centered?: boolean;
}
export declare const Tabs: FC<Props>;
export default Tabs;
//# sourceMappingURL=index.d.ts.map